# QuadrupleCLIPLoader (Dynamic VRAM)

Loads four text encoders through ComfyUI's DynamicVRAM path.

Use this node when DynamicVRAM is enabled and your workflow requires four CLIP/text-encoder inputs.
